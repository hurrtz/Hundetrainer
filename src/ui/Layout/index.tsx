import styled from 'styled-components/native';
import { ScrollView } from 'react-native';
import { Screen } from '@shoutem/ui';

export const StandardView = styled(ScrollView)`
  padding: 10px;
`;

export const StandardScreen = styled(Screen)`
  padding: 10px;
`;
