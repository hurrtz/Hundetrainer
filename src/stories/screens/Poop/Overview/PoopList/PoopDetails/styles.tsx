import { Left, Right, Body } from 'native-base';
import styled from 'styled-components/native';

export const StyledBody = styled(Body)`
  margin-left: 15px;
`;

export const StyledLeft = styled(Left)`
  width: 20px;
  max-width: 20px;
`;

export const StyledRight = styled(Right)`
  width: 20px;
  max-width: 20px;
`;
